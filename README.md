# Route Planner API

A simple Node.js API for route planning using the OpenRouteService API.

## 🚀 Features

- Exposes an API endpoint to calculate routes
- Uses OpenRouteService for routing data
- Lightweight Express server
- Easy to configure with environment variables

## 🛠 Tech Stack

- Node.js
- Express
- OpenRouteService API

## 📦 Setup

1. Clone the repository:
   ```bash
   git clone https://github.com/Indieparade/route-planner-api.git
   cd route-planner-api
   ```

2. Install dependencies:
   ```bash
   npm install
   ```

3. Create your environment file:
   ```bash
   cp .env.example .env
   ```

4. Add your API key to `.env`

5. Start the server:
   ```bash
   npm start
   ```

## 🔐 Environment Variables

See `.env.example` for required variables.

## ⚠️ Security

Never commit your real `.env` file or API keys to GitHub.  
This repository only contains an example config file.

## 📄 Licence

This project is for learning / prototype purposes.
